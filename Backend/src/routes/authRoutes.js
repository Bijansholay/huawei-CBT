const express = require('express')
const router = express.router()
const multer = require('multter')
router.get('/exam/:id', (res, req) => {

})
router.post('/exams/:id/submit', (req, res) => {

})

exports.module = router